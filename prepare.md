# Prepare — goal-drift-alert 決策記錄

> **版本**：AG-008 · 2026-08-10
> **載入時機**：本專案的新功能討論 / 架構設計 / 技術選型。
> 記錄規則繼承主線 `prepare.md`，此處只記差異與落地細節。
> **不複製主線決策的理由**，只寫落地細節 ＋ `← D-00X` 指標。

---

## 決策日誌（新的在上）

### AG-008 · 2026-08-10 · M1 實作落地

M0 是「先寫文件不寫程式」，M1 第一版把文件變成可執行的東西時，有五處必須自己定案。
以下每一筆都是**文件沒寫、實作躲不掉**的問題。

**① 偵測器只上 D2／D4／D5，D1 與 D3 延到 M2**

- **理由**：D1 季節殘差要 `statsmodels`（M0 自己把它列為 M2 依賴），且 `cold_start.min_points.seasonal`
  是 42 天，套在 31 天的月目標上整個 M1 都會停在校準中；D3 比率型要漏斗分母，
  而 `progress.csv` 的三欄 schema 裡根本沒有分母欄位。兩者現在做出來都**無法驗證**，
  而無法驗證的偵測器是把假警報寫進地基
- **落地**：`docs/ARCHITECTURE.md` 六個偵測器表的 M1 欄同步改正，不留「文件說有、程式沒有」的落差
- **棄選**：為了湊滿表格先做 D1（多裝一個 M2 依賴，換來一個永遠在校準中的偵測器）

**② 模組表新增第六層 `core/`（參數載入 ＋ 資料模型）**

- **理由**：參數載入放 `compute` 的話 `gate` 就得反向 import `compute`，
  而那條 import 方向正是「gate 不計算指標」這條界線的實體。共用且**不含任何判斷**的東西
  獨立成一層，界線才守得住 ← `AG-003`／`docs/ARCHITECTURE.md` 模組職責表

**③ 無金額場景的損失尺度：新增 `gate.interrupt_only_loss_units = 80`**

- **理由**：V 同時乘上「剩餘時間比例」與「可救性」，而這兩項在可行性翻轉的那一刻**都已遠小於 1**
  （剩 8 天／共 31 天 = 0.26；可救性 0.3）。所以損失項的尺度不是美感問題，
  是**系統會不會說話**的問題。校準準則因此不取「感覺多少」，而取
  「月目標在可行性翻轉那一刻 V 必須為正」——那是本系統唯一保證會說話的時刻
- **已知限制（記為 M3 回放的第一個檢查項）**：無金額時跨目標的相對重要性表達不出來，
  「3 篇文章缺 2 篇」與「60 份投遞缺 24 份」的 shortfall_ratio 可以一樣大，
  而後者顯然重要得多。要分辨只能靠使用者填 `unit_value`。
  **這正是關卡 2 要抓的價值錯誤：統計完全正確，但排序不符合情境**
- **棄選**：把損失直接設成缺口絕對值（單位不同的目標會變成比大小，刷題 100 題永遠壓過投遞 60 份）

**④ `deadline == start_date` 是合法的，契約檢查改為只擋 `deadline < start`**

- **理由**：日目標的起訖本來就是同一天。照 `docs/DATA-CONTRACT.md` 原本寫的 `deadline > start_date`，
  M0 自己附的範例資料（`apply_daily`）第一個就會被擋下來——**規則與自己的範例互相矛盾**
- **落地**：`DATA-CONTRACT.md` 與 `DECISION-FLOW.md` 關卡 0 的敘述同步改，
  並在 `Goal.remaining_days` 明寫「兩端含頭含尾」的理由（最後一天不能除以零）

**⑤ `min_points.monte_carlo = 30` 撞上 31 天的月目標——不改參數，改門檻的作用範圍**

- **現象**：月目標最多 31 個資料點，等於整個月的前 29 天都給不出達成機率
- **決策**：資料量門檻只擋「**該偵測器能不能用**」，不擋「這則警示能不能發」。
  D4 靠 `min_points.historical_best`（新增，21 天）、D5 靠 `ewma`（20 點），各自在偵測層擋；
  Monte Carlo 不足時卡片照發，但**三個選項的機率一律標「—」並寫明還差幾天**
- **理由**：門檻存在的目的是不畫一條假裝可信的線，不是讓系統閉嘴。
  真正的校準要等 M3 的 90 天回放；現在調參數只是拿一組沒被驗證的數字換另一組
- **落地**：沒有模擬時 P(風險為真) 用 `1 − 目前速度 ÷ required`，
  不用 `缺口 ÷ target`——後者在期初就會給出嚇人的數字，而期初缺口大是正常的，不是風險

### AG-007 · 2026-08-07 · 命名

- **決策**：專案由 `alertgate` 更名為 **`goal-drift-alert`**（遠端 repo、本機資料夾、
  所有文件引用同步更換）。更名發生在 v0.1.0 推出後的當天，**走正規分支 → PR，不 force push main**
- **理由**：`alertgate` 是機制名（閘門），讀者要先讀完 `DECISION-FLOW.md` 才知道它在管什麼；
  以**現有功能**命名才找得到、也說得清。`drift`（偏移）是本專案實際偵測的東西——
  **每天都在正常範圍內、累積起來卻到不了**，這正是異常偵測結構上抓不到的那一類
- **為什麼不是 `goal-alert`**：本專案 `CLAUDE.md` 明寫「不做提醒功能」，
  而 `goal-alert` 會被讀成到期提醒 App，**恰好是我們刻意棄選的那個東西**。
  多一個 `drift` 換掉這個誤解，值得
- **棄選**：維持 `alertgate`（不用動任何引用，但抽象到搜尋不到、也講不清）；
  `goal-alert`（短，但語意撞上已棄選的提醒工具）；`drift-gate`（保留閘門概念，
  但沒有 `goal` 就不知道在偏移什麼）
- **落地**：`gh repo rename`；本機 `mv`；`README.md`／`CLAUDE.md`／`prepare.md`／
  `config/params.yaml`／`docs/ARCHITECTURE.md` 五個檔案的引用一併替換。
  **根 repo 的分支名 `docs/alertgate-init-twentysecond` 刻意不改**——它已推送且 PR #60 已開，
  改名要關 PR 重開，成本高於收益；分支名是歷史，不是介面

### AG-006 · 2026-08-07 · ux

- **決策**：Dashboard 走 **Python ＋ Jinja2 產生單一靜態 HTML ＋ 手寫 inline SVG**，
  主視圖是 **CCPM fever chart**（所有目標一張圖），不是每個目標一張 burn-up
- **理由**：burn-up 一個目標一張，看五個目標要看五張圖，而人不會看五張圖。
  fever chart 是唯一能把所有目標放在同一平面比較的形式，且三色區直接對應「可救性」
- **落地**：fever chart 要畫**過去 7 天的軌跡尾巴**——「正在往紅區走」與「已在紅區但穩住」
  正確動作完全不同，靜態的點分不出來
- **棄選**：Streamlit（需要一個活著的 process，會在最需要時剛好沒開）；
  plotly（3 MB JS 換五種圖不划算，且擋掉「直接當 artifact 發布」這條路）
- **邊界**：本檔與 `docs/DASHBOARD.md` **刻意不寫死配色**，M4 實作時依 `dataviz` 規範定案

### AG-005 · 2026-08-07 · 待辦與目標的連結

- **決策**：待辦警示**不是獨立管線**，與目標偏移**共用同一個決策閘與同一份每日配額**。
  三條連結規則：①結構（`tasks.goal_id`）②向上彙總（待辦延遲推高父目標 required rate）
  ③向下歸因（目標觸發可行性翻轉時，決策卡**必須**列出貢獻最大的 2–3 件待辦）
- **理由**：若兩者各有配額，總量翻倍，警示疲勞照樣發生——**共用配額才是「注意力是預算」的實體**。
  而向下歸因是 SRE「每則警示必須可行動」的要求：只說「來不及了」不說「卡在哪」的警示不該發
- **落地**：計算順序固定 `tasks → goals`，所以**待辦延遲是目標可行性的領先指標**
- **棄選**：待辦與目標各自獨立輸出（實作簡單，但兩個系統各發各的等於沒有預算概念）

### AG-004 · 2026-08-07 · 資料接入

- **決策**：Notion 當 M1 唯一資料源；因「職缺搜集情況」的 **status 變更沒有時間戳**，
  用**每日 23:00 snapshot 的差分**還原流量。同時建議在 Notion 加「投遞日期」欄位，
  兩者並行、有值時以欄位為準，差異記入 sync 日誌當資料品質指標
- **理由**：存量拿得到（現在有幾筆已投遞）、流量拿不到（今天投了幾筆），而目標偏移要的是流量。
  只做欄位要等它上線後才有資料；只做 snapshot 則精度受限於一天一次。兩條並行才能
  **既覆蓋空窗、又逐步變精確**
- **落地**：漏跑一天的第一筆差分會把兩天的量記在一起 → `progress.source` 必須標
  `notion_diff_gap`，否則 run rate 會出現假峰值
- **棄選**：等使用者改完 Notion 再開工（把開工時間押在一個人工步驟上）；
  改用 Notion 版本歷史 API（拿不到 status 變更的逐筆時間戳）

### AG-003 · 2026-08-07 · 偵測層

- **決策**：偵測器固定六個（D1 季節殘差／D2 計數／D3 比率／D4 pacing／D5 變點／D6 生存），
  **M1 只做 D1–D5，D6 排 M2**；**v1 不引入任何 ML 模型**，只有統計 ＋ 一個校準器
- **理由**：監控要的是穩健、可解釋、不需重訓。**模型越強越容易把異常正常化**——
  適應性強的模型會把上個月的異常學成「這條線本來就長這樣」，然後從此不再報。
  另一層理由是可解釋性：使用者判斷「這則對不對」是唯一無法自動化的環節（← AG-002 的關卡 2），
  黑箱會直接剝奪這個能力
- **落地**：D5 **永遠與其他並聯**——D1–D4 問「今天偏離正常嗎」，只有 D5 問「正常本身變了嗎」，
  後者是前者結構上抓不到的（滾動基線會把新常態吸收進去然後安靜下來）。
  D3 的 `min_denominator: 100` 是最大宗假警報的來源，不可調鬆
- **棄選**：AutoML／agent 自動選型（沒有標籤資料就無法驗證選得好不好，且會把作品重心
  從「決策」偷換成「選模型」）；Prophet（依賴重，對這個資料量沒有優勢，留作 M3 對照組）

### AG-002 · 2026-08-07 · 決策層

- **決策**：核心公式加入**可救性**項——`V = P × 損失 × 時間衰減 × 可救性 − 打斷成本`。
  required rate 超過歷史最佳 × 1.5 時可救性歸零，**該目標不再發警示，改發「建議改目標」**
- **理由**：沒有這一項，系統會在最後三天每天提醒你一件已經做不到的事——
  那是最典型也最快讓人關掉通知的失敗模式。**認賠出場是風險管理的正當動作，不是失敗**
- **落地**：三個不該警示的時刻寫進 `gate` 而非靠人記得——期初（時間消耗比 < 20%）、
  剛達成里程碑後、只是落後但 required 仍在正常範圍。
  正確性驗證走**回放**不走單元測試（見 `docs/DECISION-FLOW.md` 關卡 2）
- **棄選**：純 z-score 或固定百分比門檻（統計顯著 ≠ 值得打斷你）；
  每個目標各自設門檻（等於把技術問題丟給使用者，而使用者答不出 EWMA 的 lambda 該設多少）

### AG-001 · 2026-08-07 · 起點選擇

- **決策**：M1 做**目標偏移**（資料源＝自己的求職記錄），**不做廣告投放**；
  待辦風險排 M2、通用化模板排 M5
- **理由**：目標偏移是唯一一個**本人有直覺的切角**——判斷得出「這則警示對不對」，
  而那正是唯一無法自動化的環節。且它與廣告 pacing **是同一組公式**
  （`pace ratio` ＝ spend pacing、`SPI` ＝ 預算執行率、`CPI` ＝ ROAS 的結構），
  換掉 `goals` 內容與 `progress` 來源即可長成廣告版，程式碼一行不用改
- **落地**：資料門檻低到可以立刻開工——pace ratio 與 SPI 只要「一個目標 ＋ 14 天記錄」；
  reference class 先驗可**回溯補填 5 筆歷史目標**，不必等 4 個月
- **棄選**：直接做廣告版（名詞與直覺都要現學，會做出一個統計正確但自己判斷不出對錯的東西）；
  先做待辦（生存分析要 100 個完成事件、驗證「改期三次斷崖」要 500–800 筆，得等 4–8 個月）

### AG-000 · 2026-08-07 · 立項

- **決策**：立項為作品集專案（← 主線 `D-020`），**repo public 但資料一律不進版控**——
  `data/` 除 `data/examples/` 外全部 gitignore
- **理由**：這裡會存個人求職記錄與待辦內容。public 是因為它要進作品集、要被看到；
  資料不進版控是因為**一筆真實資料進了 public repo 就永久拔不乾淨**。兩者不衝突，
  但必須在第一個 commit 之前就設好 `.gitignore`，補救的成本是重寫歷史
- **落地**：技術棧 Python 3.11+，依賴上限 `pandas`／`numpy`／`PyYAML`／`Jinja2`／`notion-client`；
  儲存 M1 用 CSV（人眼直接讀得懂、改得動），M2 再換 SQLite
- **棄選**：private repo（作品集看不到就沒有意義）；資料進版控但打碼（打碼過的資料
  既不能用來驗證、又還是洩漏了結構與時間軸）
